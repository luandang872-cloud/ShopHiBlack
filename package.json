{
  "name": "hi-black-shop",
  "version": "1.0.0",
  "private": true,
  "description": "Hí Black Shop iOS app",
  "scripts": {
    "sync": "npx cap sync ios",
    "open:ios": "npx cap open ios",
    "build:ios": "npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.0",
    "@capacitor/ios": "^7.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0"
  }
}